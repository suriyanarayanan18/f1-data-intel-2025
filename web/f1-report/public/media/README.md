# Local Media

Drop local assets here. The app resolves these candidate paths automatically:

- /media/drivers/<slug>.webp|png|jpg|jpeg`n- /media/teams/<slug>.webp|png|jpg|jpeg`n- /media/cars/<slug>.webp|png|jpg|jpeg

If a file is missing, the UI renders a fallback tile and does not crash.
